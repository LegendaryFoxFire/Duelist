//
//  DatabaseIdeas.txt
//  Duelist
//
//  Created by John Bukoski on 7/15/25.
//

add volume, theme, notification fields to schema
add profile picture using firebase store OR an alternative 

    func downloadProfileImage(path: String, completion: @escaping (UIImage?) -> Void) {
        let imageRef = storage.reference(withPath: path)
        
        imageRef.getData(maxSize: 5 * 1024 * 1024) { data, error in
            if let error = error {
                print("Error downloading image: \(error.localizedDescription)")
                completion(nil)
                return
            }
            
            if let data = data, let image = UIImage(data: data) {
                completion(image)
            } else {
                completion(nil)
            }
        }
    }
